import { observer } from "mobx-react-lite";

type TFooterButtons = 'search' | 'manage' | 'collection'

export const Footer = observer(({ buttons }) => {
    return (
        <footer>

        </footer>
    );
})